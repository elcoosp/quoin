# quoin-expand-test

A minimal crate used to inspect macro expansion output during development.
Not part of the main workspace; run manually with `cargo expand` to debug
`quoin_render!` and other proc macros.
