[package]
name = "counter-floem"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
floem = "0.2.0"
counter-lib = { path = "../counter", features = ["floem"] }
quoin-floem = { path = "../../quoin-floem" }
quoin = { path = "../../quoin" }
