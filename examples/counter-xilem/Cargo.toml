[package]
name = "counter-xilem"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
xilem = "0.4.0"
counter-lib = { path = "../counter" }
quoin = { path = "../../quoin", features = ["xilem"] }
winit = "0.30"
